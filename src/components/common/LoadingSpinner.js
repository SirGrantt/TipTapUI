import React from 'react';
import ReactLoading from 'react-loading';

const cylon = "cylon";
const color = "4682b4";

const Loader = () => (
    <ReactLoading type="cylon" color="#4682b4" height={600} width ={400} />
);

export default Loader;
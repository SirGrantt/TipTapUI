import React from 'react';
import ReactLoading from 'react-loading';

const Loader = () => (
    <ReactLoading type="cylon" color="#4682b4" height={600} width={400} className='loader'/>
);

export default Loader;